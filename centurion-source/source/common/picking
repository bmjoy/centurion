#pragma once

#include <headers>

using namespace std;
using namespace glm;

namespace glb {
	
	extern int leftClickID;
	extern int rightClickID;

	static int pickingID = 1;
	void increasePickingID();
	int getPickingID();

	static map<int, string> pickingListUI;
	void addValueToPickingListUI(int pick, string value);
	string getPickedObjectName(int pick);
	void resetPicking();
	int get_id(string LeftRight = "left");
	vec3 getPickingColorFromID(int pickingID);
	
	//Double clicking section
	struct doubleClickData {
		double lastTime = 0;
		int clickCount = 0;
		int clickIdList[2] = { 0, 0 };
	};
	static doubleClickData dCD;
	void resetDoubleClickTime();
	bool hasDoubleClicked();
};