#pragma once

#include <headers>

using namespace std;
using namespace glm;

namespace glb {

	static int pickingID = 1;
	void increasePickingID();
	int getPickingID();

	static map<int, string> pickingListUI;
	void addValueToPickingListUI(int pick, string value);
	string getPickedObjectName(int pick);
	void resetPicking();
	int get_id();
	vec3 getPickingColorFromID(int pickingID);
};